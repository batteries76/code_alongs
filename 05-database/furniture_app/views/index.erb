

<h1>All Items</h1>
      <% @items.each do |item| %>
  <p>
    <a href="/item/<%= item.id %>">
      <%= item.name %>
    </a>
  </p>
  <a href="/item/<%= item.id %>">
    <img src="<%= item.image_path %>" alt="">
  </a>
      <% end %>

<div class="button"> Hello Test </div>
